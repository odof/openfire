<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="of_crm_crm_stage_form" model="ir.ui.view">
        <field name="name">of.crm.crm.stage.form</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='requirements']" position="after">
                <group name="stage_automation" string="Stage automation" colspan="4" groups="of_base.of_group_root_only">
                    <group col="2">
                        <field name="of_auto_model_name"/>
                        <field name="of_auto_field_id" options="{'no_create': True, 'no_open': True}" domain="[('model', '=', of_auto_model_name), ('store', '=', True)]" attrs="{'required': [('of_auto_model_name', '!=', False)]}"/>
                        <field name="of_auto_comparison_code" attrs="{'required': [('of_auto_model_name', '!=', False)]}"/>
                    </group>
                    <group col="2">
                        <field name="of_manual_activity_id"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>

</odoo>
