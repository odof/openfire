<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="of_mail_activity_type_view_form" model="ir.ui.view">
        <field name="name">of.crm.mail.activity.type.view.form</field>
        <field name="model">mail.activity.type</field>
        <field name="inherit_id" ref="mail.mail_activity_type_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='category']" position="after">
                <field name="of_activities_type" widget="many2many_tags"
                        attrs="{'invisible': [('category','!=','activities_lot')], 'required': [('category','=','activities_lot')]}"
                        domain="[('category','!=','activities_lot')]"/>
            </xpath>
        </field>
    </record>

</odoo>
