<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="calendar_event_view_form" model="ir.ui.view">
        <field name="name">calendar_event.view.form</field>
        <field name="model">calendar.event</field>
        <field name="inherit_id" ref="of_equipment.calendar_event_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='of_equipment_ids']" position="before">
                <field name="of_equipment_ids_domain" invisible="1" />
            </xpath>
        </field>
    </record>

</odoo>
