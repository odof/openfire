<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="of_account_tax_view_order_form" model="ir.ui.view">
        <field name="name">of.account.tax.sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='other_information']/group/group[@name='sale_info']//field[@name='fiscal_position_id']/.." position="replace"/>
            <xpath expr="//page[@name='other_information']/group/group[@name='sale_info']//label[@for='fiscal_position_id']" position="replace"/>
            <xpath expr="//field[@name='date_order']" position="after">
                <label for="fiscal_position_id"/>
                <div class="o_row">
                    <field name="fiscal_position_id" options="{'no_create': True}"/>
                    <button name="action_update_taxes" type="object"
                        string=" Update Taxes"
                        help="Recompute all taxes based on this fiscal position"
                        class="btn-link mb-1 px-0" icon="fa-refresh"
                        confirm="This will update all taxes based on the currently selected fiscal position."
                        attrs="{'invisible': ['|', ('show_update_fpos', '=', False), ('state', 'in', ['sale', 'done','cancel'])]}"/>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
