<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_partner_form_inherit_res" model="ir.ui.view">
        <field name="name">res.partner.view.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Scripts">
                    <field name="script_ids"/>
                </page>
                <page string="Databases">
                    <field name="database_ids" readonly="1"/>
                </page>
                <page string="Migrations">
                    <field name="migration_ids" readonly="1"/>
                </page>
            </xpath>
        </field>
    </record>

    <record id="migration_partner_action" model="ir.actions.act_window">
        <field name="name">Partner</field>
        <field name="res_model">res.partner</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
    </record>

</odoo>
