<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="of_map_view.ListPopupMap" owl="1">
        <div class="map--list-popup" t-attf-style="width: {{archInfo.width + 15}}px;">
            <t t-foreach="state.records" t-as="record" t-key="record.id">
                <PopupMap t-props="rendererProps" record="record" archInfo="archInfo"/>
            </t>
        </div>
    </t>

    <t t-name="of_map_view.PopupMap" owl="1">
        <t t-if="state.show">
            <div class="map-popup m-2 position-relative" t-on-mouseenter="onMouseOver" t-on-mouseleave="onMouseOut" t-attf-style="background-color: {{state.color}}; width: {{archInfo.width}}px; ">
                <div class="map-popup--header position-absolute pe-1" t-on-click="onClose" >
                    <i class="fa fa-close"/>
                </div>
                <div id="popup-content" class="map-popup--content m-2 me-3" t-on-click.stop.prevent="onClick">
                    <t t-out="getPopoverProps(props)" />
                </div>
            </div>
        </t>
    </t>

</templates>
