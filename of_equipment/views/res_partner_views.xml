<?xml version='1.0' encoding='utf-8'?>
<odoo>

    <record id="res_partner_view_form" model="ir.ui.view">
        <field name="name">res.partner.view.form</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_button_view_equipment" type="object" class="oe_stat_button" icon="fa-home" context="{'search_default_customer_id': active_id}">
                    <field name="of_equipments_count" string="Equipments" widget="statinfo"/>
                </button>
            </xpath>
            <xpath expr="//group[@name='sale']/field[@name='user_id']" position="after">
                <field name="of_is_reseller"/>
                <field name="of_is_installer"/>
            </xpath>
        </field>
    </record>

</odoo>
