<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- /shop/product product page -->
    <template id="product" inherit_id="website_sale.product" name="Product">
        <xpath expr="//form[@action='/shop/cart/update']" position="after">
            <form t-if="product.sudo().sample_id" action="/shop/cart/sample_update" class="js_add_sample_cart_variants" t-att-data-attribute_value_ids="attribute_value_ids" method="POST" id="form_sample_to_cart">
                <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()" />
                <input type="hidden" class="product_id" name="product_id" t-att-value="product.sudo().sample_id and product.sudo().sample_id.product_variant_id.id"/>
                <a id="add_sample_to_cart" class="btn btn-primary btn-lg mt8 a-submit" href="#">Add sample to Cart</a>
            </form>
        </xpath>
    </template>

    <!-- /shop/cart page -->
    <template id="cart_lines" inherit_id="website_sale.cart_lines" name="Sample Shopping Cart Lines">
        <xpath expr="//a[@t-attf-href='/shop/product/#{ slug(line.product_id.product_tmpl_id) }']" position="attributes">
            <attribute name="t-attf-href">#{line.product_id.product_tmpl_id.is_sample and '/shop/product/'+slug(line.product_id.product_tmpl_id.sample_parent_id) or '/shop/product/'+slug(line.product_id.product_tmpl_id)}</attribute>
        </xpath>
        <xpath expr="//input" position="attributes">
            <attribute name="t-attf-data-max">#{line.product_id.product_tmpl_id.is_sample and '1'}</attribute>
        </xpath>
    </template>

    <!-- Code utilisé si les sample sont en website_published. On part sur un non -->
<!--    <template id="products" inherit_id="website_sale.products" name="Products">-->
<!--        <xpath expr="//div[@class='oe_product_cart']" position="attributes">-->
<!--            <attribute name="t-att-data-publish">product.website_published and not product.is_sample and 'on' or 'off'</attribute>-->
<!--            <attribute name="t-if">not product.is_sample</attribute>-->
<!--            <attribute name="t-att-product">product</attribute>-->
<!--        </xpath>-->
<!--    </template>-->

</odoo>
