<?xml version="1.0"?>
<odoo>

    <data noupdate="1">
        <record id="of_sms_planning_customer_appointment_reminder" model="sms.template">
            <field name="name">SMS Customer appointment reminder</field>
            <field name="model_id" ref="calendar.model_calendar_event"/>
            <field name="body">${object.company_id.name}
Reminder : You have an appointment on ${object.date and format_date(object.date) or ""}.</field>
        </record>
    </data>

</odoo>
